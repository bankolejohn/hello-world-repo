name: Hello World

on: push

jobs:

    hello-job:

        runs-on: ubuntu-latest 

        steps:

        - name: checkout code
          uses: actions/checkout@v3    
        - name: Echo text
          run: echo "Hello, world!"
